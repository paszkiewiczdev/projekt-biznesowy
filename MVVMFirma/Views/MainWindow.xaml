<Window x:Class="MVVMFirma.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MVVMFirma.Views"
        Title="Studio KASS – system faktur i magazynu"
        Height="300"
        Width="300"
        WindowState="Maximized"
        Background="{DynamicResource KassBackgroundBrush}">

    <!-- słownik ze stylami i powiązaniami -->
    <Window.Resources>
        <ResourceDictionary Source="MainWindowResources.xaml" />
    </Window.Resources>

    <!-- główny layout -->
    <Grid Background="{StaticResource KassBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <!-- menu gorne -->
        <Menu Grid.Column="0"
              Grid.Row="0"
              Grid.ColumnSpan="2"
              Margin="0"
              Background="{StaticResource KassPrimaryDarkBrush}"
              Foreground="White">
            <MenuItem Header="_Plik" />

            <MenuItem Header="_Magazyn">
                <MenuItem Header="Wszystkie towary" />
                <MenuItem Header="Nowy towar" />
                <MenuItem Header="Wydanie Zewnętrzne (WZ)" />
                <MenuItem Header="Przyjęcie Zewnętrzne (PZ)" />
            </MenuItem>

            <MenuItem Header="_Program" />
        </Menu>

        <!-- toolbar i ikony -->
        <ToolBar Grid.Column="0"
                 Grid.Row="1"
                 Grid.ColumnSpan="2"
                 Margin="0"
                 Background="{StaticResource KassPrimaryBrush}">
            <ToggleButton Style="{StaticResource IconToggleButtonStyle}" />
            <ToggleButton Style="{StaticResource IconToggleButtonStyle}" />
        </ToolBar>

        <!-- lewy panel i operacje -->
        <Border Grid.Row="2"
                Grid.Column="0"
                Margin="4,4,4,4"
                Style="{StaticResource MainBorderStyle}">
            <HeaderedContentControl
                Header="Operacje"
                Content="{Binding Path=Commands}"
                ContentTemplate="{StaticResource CommandsTemplate}"
                Style="{StaticResource MainHCCStyle}" />
        </Border>

        <!-- prawy panel - roboczy -->
        <Border Grid.Row="2"
                Grid.Column="1"
                Margin="0,4,4,4"
                Style="{StaticResource MainBorderStyle}">
            <HeaderedContentControl
                Header="Przestrzeń robocza"
                Content="{Binding Path=Workspaces}"
                ContentTemplate="{StaticResource WorkspacesTemplate}"
                Style="{StaticResource MainHCCStyle}" />
        </Border>

        <!-- status bar na dole -->
        <StatusBar Grid.Column="0"
                   Grid.Row="3"
                   Grid.ColumnSpan="2"
                   Background="{StaticResource KassPrimaryDarkBrush}"
                   Foreground="White">
            <StatusBarItem>
                <TextBlock Text="Studio KASS" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="WSB-NLU" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
