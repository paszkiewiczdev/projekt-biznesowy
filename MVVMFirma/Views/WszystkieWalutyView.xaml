<UserControl x:Class="MVVMFirma.Views.WszystkieWalutyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding List}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding IdWaluty}"
                                    Width="80" />
                <DataGridTextColumn Header="Kod"
                                    Binding="{Binding Kod}"
                                    Width="*" />
                <DataGridTextColumn Header="Nazwa"
                                    Binding="{Binding Nazwa}"
                                    Width="2*" />
                <DataGridTextColumn Header="Kurs"
                                    Binding="{Binding Kurs}"
                                    Width="*" />
                <DataGridCheckBoxColumn Header="Domyślna"
                                        Binding="{Binding CzyDomyslna}"
                                        Width="100" />
                <DataGridCheckBoxColumn Header="Aktywna"
                                        Binding="{Binding CzyAktywna}"
                                        Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
