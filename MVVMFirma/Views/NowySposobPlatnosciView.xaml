<vw:JedenViewBase x:Class="MVVMFirma.Views.NowySposobPlatnosciView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:vw="clr-namespace:MVVMFirma.Views"
                  mc:Ignorable="d"
                  d:DesignHeight="300"
                  d:DesignWidth="500">

    <Border Margin="8" Padding="8" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"   />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"   />
            </Grid.ColumnDefinitions>


            <TextBlock Grid.Row="0" Grid.Column="0"
                       Margin="0,0,8,4"
                       VerticalAlignment="Center"
                       Text="Nazwa:"/>
            <TextBox  Grid.Row="0" Grid.Column="1"
                      Margin="0,0,0,4"
                      Text="{Binding Nazwa, UpdateSourceTrigger=PropertyChanged}"/>


            <TextBlock Grid.Row="1" Grid.Column="0"
                       Margin="0,0,8,4"
                       VerticalAlignment="Top"
                       Text="Opis:"/>
            <TextBox  Grid.Row="1" Grid.Column="1"
                      Margin="0,0,0,4"
                      Height="60"
                      AcceptsReturn="True"
                      TextWrapping="Wrap"
                      Text="{Binding Opis, UpdateSourceTrigger=PropertyChanged}"/>


            <CheckBox Grid.Row="2" Grid.ColumnSpan="2"
                      Margin="0,4,0,4"
                      Content="Aktywny"
                      IsChecked="{Binding CzyAktywny}"/>


            <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                        Margin="0,12,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Zapisz"
                        Width="90"
                        Margin="0,0,8,0"
                        Command="{Binding ZapiszCommand}"/>
                <Button Content="Anuluj"
                        Width="90"
                        Command="{Binding AnulujCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</vw:JedenViewBase>
