<local:JedenViewBase x:Class="MVVMFirma.Views.NowaPozycjaFakturySprzedazyView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:MVVMFirma.Views"
                     mc:Ignorable="d"
                     d:DesignHeight="520"
                     d:DesignWidth="520">
    <local:JedenViewBase.Template>
        <ControlTemplate TargetType="{x:Type local:JedenViewBase}">
            <Grid Margin="0,10,0,0">
                <ContentPresenter Margin="0,5,0,5"/>
            </Grid>
        </ControlTemplate>
    </local:JedenViewBase.Template>

    <local:JedenViewBase.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </local:JedenViewBase.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Faktura" Margin="0,6,8,6"/>
            <ComboBox Grid.Column="1" Grid.Row="0" Margin="0,6,0,6"
                      ItemsSource="{Binding Faktury}"
                      DisplayMemberPath="Numer"
                      SelectedItem="{Binding SelectedFaktura, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="1" Content="Towar" Margin="0,6,8,6"/>
            <ComboBox Grid.Column="1" Grid.Row="1" Margin="0,6,0,6"
                      ItemsSource="{Binding Towary}"
                      DisplayMemberPath="Nazwa"
                      SelectedItem="{Binding SelectedTowar, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="2" Content="Stawka VAT" Margin="0,6,8,6"/>
            <ComboBox Grid.Column="1" Grid.Row="2" Margin="0,6,0,6"
                      ItemsSource="{Binding StawkiVat}"
                      DisplayMemberPath="Wartosc"
                      SelectedItem="{Binding SelectedStawkaVat, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="3" Content="Ilość" Margin="0,6,8,6"/>
            <TextBox Grid.Column="1" Grid.Row="3" Margin="0,6,0,6"
                     Text="{Binding IloscText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="4" Content="Cena netto" Margin="0,6,8,6"/>
            <TextBox Grid.Column="1" Grid.Row="4" Margin="0,6,0,6"
                     Text="{Binding CenaNettoText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="5" Content="Wartość netto" Margin="0,6,8,6"/>
            <TextBox Grid.Column="1" Grid.Row="5" Margin="0,6,0,6"
                     IsReadOnly="True"
                     Text="{Binding WartoscNettoText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="6" Content="Wartość VAT" Margin="0,6,8,6"/>
            <TextBox Grid.Column="1" Grid.Row="6" Margin="0,6,0,6"
                     IsReadOnly="True"
                     Text="{Binding WartoscVatText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Label Grid.Column="0" Grid.Row="7" Content="Wartość brutto" Margin="0,6,8,6"/>
            <TextBox Grid.Column="1" Grid.Row="7" Margin="0,6,0,6"
                     IsReadOnly="True"
                     Text="{Binding WartoscBruttoText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
        </Grid>
    </ScrollViewer>
</local:JedenViewBase>