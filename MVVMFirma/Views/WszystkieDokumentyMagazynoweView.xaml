<UserControl x:Class="MVVMFirma.Views.WszystkieDokumentyMagazynoweView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding DokumentyMagazynowe}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nr dokumentu"
                                    Binding="{Binding Numer}" />

                <DataGridTextColumn Header="Data dokumentu"
                                    Binding="{Binding DataDokumentu}" />

                <DataGridTextColumn Header="Magazyn"
                                    Binding="{Binding Magazyn.Nazwa}" />

                <DataGridTextColumn Header="Typ dokumentu"
                                    Binding="{Binding TypDokumentuMagazynowego.Nazwa}" />

                <DataGridTextColumn Header="Uwagi"
                                    Binding="{Binding Uwagi}" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
