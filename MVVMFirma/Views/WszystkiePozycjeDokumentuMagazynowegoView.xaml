<UserControl x:Class="MVVMFirma.Views.WszystkiePozycjeDokumentuMagazynowegoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding PozycjeDokumentuMagazynowego}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Nr dokumentu"
                                    Binding="{Binding DokumentMagazynowy.NrDokumentu}" />

                <DataGridTextColumn Header="Data dokumentu"
                                    Binding="{Binding DokumentMagazynowy.DataDokumentu}" />

                <DataGridTextColumn Header="Magazyn"
                                    Binding="{Binding DokumentMagazynowy.Magazyn.Nazwa}" />


                <DataGridTextColumn Header="Towar"
                                    Binding="{Binding Towar.Nazwa}" />

                <DataGridTextColumn Header="Ilość"
                                    Binding="{Binding Ilosc}" />

                <DataGridTextColumn Header="Cena netto"
                                    Binding="{Binding CenaNetto}" />

                <DataGridTextColumn Header="Wartość netto"
                                    Binding="{Binding WartoscNetto}" />

                <DataGridTextColumn Header="Wartość brutto"
                                    Binding="{Binding WartoscBrutto}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
