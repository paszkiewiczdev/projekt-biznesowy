<UserControl x:Class="MVVMFirma.Views.WszystkieZamowieniaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Zamowienia}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Nr zamówienia"
                                    Binding="{Binding Numer}" />


                <DataGridTextColumn Header="Data zamówienia"
                                    Binding="{Binding DataZamowienia}" />


                <DataGridTextColumn Header="Kontrahent"
                                    Binding="{Binding Kontrahent.NazwaKontrahenta}" />


                <DataGridTextColumn Header="Status"
                                    Binding="{Binding StatusZamowienia.Nazwa}" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
