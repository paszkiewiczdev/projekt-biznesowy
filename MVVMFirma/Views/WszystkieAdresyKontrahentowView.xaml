<UserControl x:Class="MVVMFirma.Views.WszystkieAdresyKontrahentowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding AdresyKontrahentow}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Kontrahent"
                                    Binding="{Binding Kontrahent.NazwaKontrahenta}" />

                <DataGridTextColumn Header="Ulica"
                                    Binding="{Binding Ulica}" />

                <DataGridTextColumn Header="Nr domu"
                                    Binding="{Binding NrDomu}" />

                <DataGridTextColumn Header="Nr lokalu"
                                    Binding="{Binding NrLokalu}" />

                <DataGridTextColumn Header="Kod pocztowy"
                                    Binding="{Binding KodPocztowy}" />

                <DataGridTextColumn Header="Miasto"
                                    Binding="{Binding Miasto}" />

                <DataGridTextColumn Header="Kraj"
                                    Binding="{Binding Kraj}" />

                <DataGridTextColumn Header="Domyślny"
                                    Binding="{Binding CzyDomyslny}" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
