<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieRaportSprzedazMiesiecznaView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:local="clr-namespace:MVVMFirma.Views"
                         mc:Ignorable="d"
                         d:DesignHeight="450"
                         d:DesignWidth="800">

    <Grid Margin="4">
        <DataGrid ItemsSource="{Binding List}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="0"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Rok"
                                    Width="70"
                                    Binding="{Binding Rok}" />

                <DataGridTextColumn Header="Miesiąc"
                                    Width="80"
                                    Binding="{Binding Miesiac}" />

                <DataGridTextColumn Header="Liczba faktur"
                                    Width="120"
                                    Binding="{Binding LiczbaFaktur}" />

                <DataGridTextColumn Header="Suma netto"
                                    Width="120"
                                    Binding="{Binding SumaNetto}" />

                <DataGridTextColumn Header="Suma VAT"
                                    Width="120"
                                    Binding="{Binding SumaVat}" />

                <DataGridTextColumn Header="Suma brutto"
                                    Width="120"
                                    Binding="{Binding SumaBrutto}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>