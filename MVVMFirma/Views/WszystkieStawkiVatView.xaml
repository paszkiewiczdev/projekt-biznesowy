<UserControl x:Class="MVVMFirma.Views.WszystkieStawkiVatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding List}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding IdStawkiVat}" 
                                    Width="80" />
                <DataGridTextColumn Header="Nazwa" 
                                    Binding="{Binding Nazwa}" 
                                    Width="*" />
                <DataGridTextColumn Header="Wartość" 
                                    Binding="{Binding Wartosc}" 
                                    Width="120" />
                <DataGridCheckBoxColumn Header="Aktywna" 
                                        Binding="{Binding CzyAktywna}" 
                                        Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
